apiVersion: v1
kind: Service
metadata:
  name: {{ include "timeline-api.fullname" . }}
  labels:
    {{- include "timeline-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "timeline-api.selectorLabels" . | nindent 4 }}
