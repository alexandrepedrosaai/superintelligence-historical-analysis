apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: superintelligence

resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml

commonLabels:
  app: timeline-api
  managed-by: kustomize

images:
  - name: superintelligenceacr.azurecr.io/superintelligence-timeline
    newTag: latest
